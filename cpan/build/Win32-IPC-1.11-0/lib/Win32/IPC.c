/*
 * This file was generated automatically by ExtUtils::ParseXS version 3.40 from the
 * contents of IPC.xs. Do not edit this file, edit IPC.xs instead.
 *
 *    ANY CHANGES MADE HERE WILL BE LOST!
 *
 */

#line 1 "lib\\\\Win32\\\\IPC.xs"
//--------------------------------------------------------------------
//
//   Win32::IPC
//   Copyright 1998 by Christopher J. Madsen
//
//   XS file for the Win32::IPC module
//
//--------------------------------------------------------------------

#include "EXTERN.h"
#include "perl.h"
#include "XSUB.h"

#include "ppport.h"

#define WIN32_LEAN_AND_MEAN
#include <windows.h>

typedef DWORD TIMEOUT;

static IV
WaitForMultiple(AV* hArray, BOOL fWaitAll, DWORD dwTimeOut)
{
  dTHX;
  int	   count;
  SV **    svpp;
  HANDLE   handle;
  HANDLE*  aHandles;
  int	   i=0;
  DWORD    result;

  count = av_len(hArray) + 1;
  if (count == 0) {
    warn("No objects to wait for");
    return IV_MAX;
  }

  New(0,aHandles,count,HANDLE);

  // Create the array of handles for the WaitForMultipleObjects call

  for (i = 0; i < count; i++) {
    svpp = av_fetch(hArray, i, 0);

    // Check if the object reference is valid

    if (!svpp) {
     invalid:
      croak("Invalid object passed ($objects[%d])",i);
      return IV_MAX;
    } else if (sv_derived_from(*svpp,"Win32::IPC")) {
      handle = INT2PTR(HANDLE, SvIV(SvRV(*svpp)));
    } else if (sv_isobject(*svpp)) {
      dSP;
      handle = INVALID_HANDLE_VALUE;
      ENTER;
      SAVETMPS;
      PUSHMARK(sp);
      XPUSHs(*svpp);
      PUTBACK;
      result = call_method("get_Win32_IPC_HANDLE", G_SCALAR|G_EVAL);
      SPAGAIN;
      if ((result == 1) && (SvIOKp(TOPs))) handle = INT2PTR(HANDLE, POPi);
      PUTBACK;
      FREETMPS;
      LEAVE;
      if (SvTRUE(ERRSV)) goto unknown;
    } else {
     unknown:
      croak("Don't know how to wait on $objects[%d]",i);
      return IV_MAX;
    }

    if (handle == INVALID_HANDLE_VALUE) goto invalid;
    aHandles[i] = handle;
  } // for loop

  // Now wait for something to happen

  result = WaitForMultipleObjects(count, aHandles, fWaitAll, dwTimeOut);
  Safefree(aHandles);

  if ((result >= WAIT_OBJECT_0) && (result < WAIT_OBJECT_0 + count))
    return result - WAIT_OBJECT_0 + 1;
  if ((result >= WAIT_ABANDONED_0) && (result < WAIT_ABANDONED_0 + count))
    return -(IV)(result - WAIT_ABANDONED_0 + 1);
  if (result == WAIT_TIMEOUT)
    return 0;
  return IV_MAX; /* error */
} /* end WaitForMultiple */

static DWORD
constant(char* name)
{
    errno = 0;
    if (strEQ(name, "INFINITE"))
      return INFINITE;
    errno = EINVAL;
    return 0;
} /* end constant */


#line 113 "lib\\Win32\\IPC.c"
#ifndef PERL_UNUSED_VAR
#  define PERL_UNUSED_VAR(var) if (0) var = var
#endif

#ifndef dVAR
#  define dVAR		dNOOP
#endif


/* This stuff is not part of the API! You have been warned. */
#ifndef PERL_VERSION_DECIMAL
#  define PERL_VERSION_DECIMAL(r,v,s) (r*1000000 + v*1000 + s)
#endif
#ifndef PERL_DECIMAL_VERSION
#  define PERL_DECIMAL_VERSION \
	  PERL_VERSION_DECIMAL(PERL_REVISION,PERL_VERSION,PERL_SUBVERSION)
#endif
#ifndef PERL_VERSION_GE
#  define PERL_VERSION_GE(r,v,s) \
	  (PERL_DECIMAL_VERSION >= PERL_VERSION_DECIMAL(r,v,s))
#endif
#ifndef PERL_VERSION_LE
#  define PERL_VERSION_LE(r,v,s) \
	  (PERL_DECIMAL_VERSION <= PERL_VERSION_DECIMAL(r,v,s))
#endif

/* XS_INTERNAL is the explicit static-linkage variant of the default
 * XS macro.
 *
 * XS_EXTERNAL is the same as XS_INTERNAL except it does not include
 * "STATIC", ie. it exports XSUB symbols. You probably don't want that
 * for anything but the BOOT XSUB.
 *
 * See XSUB.h in core!
 */


/* TODO: This might be compatible further back than 5.10.0. */
#if PERL_VERSION_GE(5, 10, 0) && PERL_VERSION_LE(5, 15, 1)
#  undef XS_EXTERNAL
#  undef XS_INTERNAL
#  if defined(__CYGWIN__) && defined(USE_DYNAMIC_LOADING)
#    define XS_EXTERNAL(name) __declspec(dllexport) XSPROTO(name)
#    define XS_INTERNAL(name) STATIC XSPROTO(name)
#  endif
#  if defined(__SYMBIAN32__)
#    define XS_EXTERNAL(name) EXPORT_C XSPROTO(name)
#    define XS_INTERNAL(name) EXPORT_C STATIC XSPROTO(name)
#  endif
#  ifndef XS_EXTERNAL
#    if defined(HASATTRIBUTE_UNUSED) && !defined(__cplusplus)
#      define XS_EXTERNAL(name) void name(pTHX_ CV* cv __attribute__unused__)
#      define XS_INTERNAL(name) STATIC void name(pTHX_ CV* cv __attribute__unused__)
#    else
#      ifdef __cplusplus
#        define XS_EXTERNAL(name) extern "C" XSPROTO(name)
#        define XS_INTERNAL(name) static XSPROTO(name)
#      else
#        define XS_EXTERNAL(name) XSPROTO(name)
#        define XS_INTERNAL(name) STATIC XSPROTO(name)
#      endif
#    endif
#  endif
#endif

/* perl >= 5.10.0 && perl <= 5.15.1 */


/* The XS_EXTERNAL macro is used for functions that must not be static
 * like the boot XSUB of a module. If perl didn't have an XS_EXTERNAL
 * macro defined, the best we can do is assume XS is the same.
 * Dito for XS_INTERNAL.
 */
#ifndef XS_EXTERNAL
#  define XS_EXTERNAL(name) XS(name)
#endif
#ifndef XS_INTERNAL
#  define XS_INTERNAL(name) XS(name)
#endif

/* Now, finally, after all this mess, we want an ExtUtils::ParseXS
 * internal macro that we're free to redefine for varying linkage due
 * to the EXPORT_XSUB_SYMBOLS XS keyword. This is internal, use
 * XS_EXTERNAL(name) or XS_INTERNAL(name) in your code if you need to!
 */

#undef XS_EUPXS
#if defined(PERL_EUPXS_ALWAYS_EXPORT)
#  define XS_EUPXS(name) XS_EXTERNAL(name)
#else
   /* default to internal */
#  define XS_EUPXS(name) XS_INTERNAL(name)
#endif

#ifndef PERL_ARGS_ASSERT_CROAK_XS_USAGE
#define PERL_ARGS_ASSERT_CROAK_XS_USAGE assert(cv); assert(params)

/* prototype to pass -Wmissing-prototypes */
STATIC void
S_croak_xs_usage(const CV *const cv, const char *const params);

STATIC void
S_croak_xs_usage(const CV *const cv, const char *const params)
{
    const GV *const gv = CvGV(cv);

    PERL_ARGS_ASSERT_CROAK_XS_USAGE;

    if (gv) {
        const char *const gvname = GvNAME(gv);
        const HV *const stash = GvSTASH(gv);
        const char *const hvname = stash ? HvNAME(stash) : NULL;

        if (hvname)
	    Perl_croak_nocontext("Usage: %s::%s(%s)", hvname, gvname, params);
        else
	    Perl_croak_nocontext("Usage: %s(%s)", gvname, params);
    } else {
        /* Pants. I don't think that it should be possible to get here. */
	Perl_croak_nocontext("Usage: CODE(0x%" UVxf ")(%s)", PTR2UV(cv), params);
    }
}
#undef  PERL_ARGS_ASSERT_CROAK_XS_USAGE

#define croak_xs_usage        S_croak_xs_usage

#endif

/* NOTE: the prototype of newXSproto() is different in versions of perls,
 * so we define a portable version of newXSproto()
 */
#ifdef newXS_flags
#define newXSproto_portable(name, c_impl, file, proto) newXS_flags(name, c_impl, file, proto, 0)
#else
#define newXSproto_portable(name, c_impl, file, proto) (PL_Sv=(SV*)newXS(name, c_impl, file), sv_setpv(PL_Sv, proto), (CV*)PL_Sv)
#endif /* !defined(newXS_flags) */

#if PERL_VERSION_LE(5, 21, 5)
#  define newXS_deffile(a,b) Perl_newXS(aTHX_ a,b,file)
#else
#  define newXS_deffile(a,b) Perl_newXS_deffile(aTHX_ a,b)
#endif

#line 257 "lib\\Win32\\IPC.c"

XS_EUPXS(XS_Win32__IPC_constant); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Win32__IPC_constant)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "name");
    {
	char*	name = (char *)SvPV_nolen(ST(0))
;
	DWORD	RETVAL;
	dXSTARG;

	RETVAL = constant(name);
	XSprePUSH; PUSHi((IV)RETVAL);
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Win32__IPC_wait_any); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Win32__IPC_wait_any)
{
    dVAR; dXSARGS;
    dXSI32;
    if (items < 1 || items > 2)
       croak_xs_usage(cv,  "objects, timeout=INFINITE");
    {
	SV *	objects = ST(0)
;
	TIMEOUT	timeout;
#line 120 "lib\\\\Win32\\\\IPC.xs"
	AV *	av;
#line 291 "lib\\Win32\\IPC.c"
	IV	RETVAL;
	dXSTARG;

	if (items < 2)
	    timeout = INFINITE;
	else {
	    timeout = (TIMEOUT)(SvOK(ST(1)) ? SvIV(ST(1)) : INFINITE)
;
	}
#line 122 "lib\\\\Win32\\\\IPC.xs"
	if (!(SvROK(objects)
	      && (av = (AV*)SvRV(objects))
	      && SvTYPE(av) == SVt_PVAV))
	    croak("First arg must be an array");
	RETVAL = WaitForMultiple(av, ix, timeout);
        if (RETVAL == IV_MAX)
          XSRETURN_UNDEF;
#line 309 "lib\\Win32\\IPC.c"
	XSprePUSH; PUSHi((IV)RETVAL);
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Win32__IPC_wait); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Win32__IPC_wait)
{
    dVAR; dXSARGS;
    if (items < 1 || items > 2)
       croak_xs_usage(cv,  "handle, timeout=INFINITE");
    {
	HANDLE	handle;
	TIMEOUT	timeout;
#line 138 "lib\\\\Win32\\\\IPC.xs"
	DWORD result;
#line 327 "lib\\Win32\\IPC.c"
	IV	RETVAL;
	dXSTARG;

	if (sv_derived_from(ST(0), "Win32::IPC")) {
	    IV tmp = SvIV((SV*)SvRV(ST(0)));
	    handle = INT2PTR(HANDLE, tmp);
	}
	else
	    croak("handle is not of type Win32::IPC")
;

	if (items < 2)
	    timeout = INFINITE;
	else {
	    timeout = (TIMEOUT)(SvOK(ST(1)) ? SvIV(ST(1)) : INFINITE)
;
	}
#line 140 "lib\\\\Win32\\\\IPC.xs"
	result = WaitForSingleObject(handle,timeout);
	if (result == WAIT_OBJECT_0)
	  RETVAL = 1;
	else if (result == WAIT_ABANDONED_0)
	  RETVAL = -1;
	else if (result == WAIT_TIMEOUT)
	  RETVAL = 0;
        else
          XSRETURN_UNDEF;
#line 355 "lib\\Win32\\IPC.c"
	XSprePUSH; PUSHi((IV)RETVAL);
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Win32__IPC_Wait); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Win32__IPC_Wait)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "handle, timeout");
    {
	HANDLE	handle;
	DWORD	timeout = (DWORD)SvIV(ST(1))
;
	DWORD	RETVAL;
	dXSTARG;

	if (sv_derived_from(ST(0), "Win32::IPC")) {
	    IV tmp = SvIV((SV*)SvRV(ST(0)));
	    handle = INT2PTR(HANDLE, tmp);
	}
	else
	    croak("handle is not of type Win32::IPC")
;
#line 158 "lib\\\\Win32\\\\IPC.xs"
    RETVAL = WaitForSingleObject(handle,timeout);
#line 384 "lib\\Win32\\IPC.c"
	XSprePUSH; PUSHi((IV)RETVAL);
    }
    XSRETURN(1);
}

#ifdef __cplusplus
extern "C"
#endif
XS_EXTERNAL(boot_Win32__IPC); /* prototype to pass -Wmissing-prototypes */
XS_EXTERNAL(boot_Win32__IPC)
{
#if PERL_VERSION_LE(5, 21, 5)
    dVAR; dXSARGS;
#else
    dVAR; dXSBOOTARGSXSAPIVERCHK;
#endif
#if (PERL_REVISION == 5 && PERL_VERSION < 9)
    char* file = __FILE__;
#else
    const char* file = __FILE__;
#endif

    PERL_UNUSED_VAR(file);

    PERL_UNUSED_VAR(cv); /* -W */
    PERL_UNUSED_VAR(items); /* -W */
#if PERL_VERSION_LE(5, 21, 5)
    XS_VERSION_BOOTCHECK;
#  ifdef XS_APIVERSION_BOOTCHECK
    XS_APIVERSION_BOOTCHECK;
#  endif
#endif

        (void)newXSproto_portable("Win32::IPC::constant", XS_Win32__IPC_constant, file, "$");
        cv = newXSproto_portable("Win32::IPC::wait_all", XS_Win32__IPC_wait_any, file, "\\@;$");
        XSANY.any_i32 = 1;
        cv = newXSproto_portable("Win32::IPC::wait_any", XS_Win32__IPC_wait_any, file, "\\@;$");
        XSANY.any_i32 = 0;
        (void)newXSproto_portable("Win32::IPC::wait", XS_Win32__IPC_wait, file, "$;$");
        (void)newXSproto_portable("Win32::IPC::Wait", XS_Win32__IPC_Wait, file, "$$");
#if PERL_VERSION_LE(5, 21, 5)
#  if PERL_VERSION_GE(5, 9, 0)
    if (PL_unitcheckav)
        call_list(PL_scopestack_ix, PL_unitcheckav);
#  endif
    XSRETURN_YES;
#else
    Perl_xs_boot_epilog(aTHX_ ax);
#endif
}

